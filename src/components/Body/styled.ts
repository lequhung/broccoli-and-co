import styled from '@emotion/styled';

export const Body = styled.div({
  minHeight: '100vh',
  margin: 0,
  padding: 0,
  display: 'flex',
  flexDirection: 'row',
  flexWrap: 'nowrap',
  width: '100%'
});
